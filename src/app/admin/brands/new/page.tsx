import { BrandForm } from '@/features/brands/components/brand-form';

export const metadata = {
  title: 'إضافة براند جديد | لوحة التحكم',
};

export default function NewBrandPage() {
  return <BrandForm />;
}
